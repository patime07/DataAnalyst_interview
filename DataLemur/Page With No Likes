WITH likes_per_page AS(
SELECT page_id, COUNT(page_id) AS likes_count
FROM page_likes
GROUP BY page_id
)

SELECT page_id
FROM pages
WHERE page_id NOT IN (SELECT page_id FROM likes_per_page);

--with EXPECT
SELECT page_id
FROM pages
EXCEPT
SELECT page_id
FROM page_likes
ORDER BY page_id;
